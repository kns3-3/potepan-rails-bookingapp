<div class="new-wrapper">
  <div class="form-box">
    <h2 class="new-title">施設編集</h2>

    <%= form_with model: @room, local: true, class: "edit-form", multipart: true do |f| %>
      
      <% if @room.errors.any? %>
        <div id="error_explanation" style="color: red; margin-bottom: 20px;">
          <ul>
            <% @room.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      
      <div class="field">
        <%= f.label :name, class: "form-label" do %>
          施設名<span class="require">*</span>
        <% end %>
        <%= f.text_field :name, class: "form-input-text", placeholder: "施設名を入力" %>
      </div>

      <div class="field">
        <%= f.label :description, class: "form-label" do %>
          施設詳細<span class="require">*</span>
        <% end %>
        <%= f.text_area :description, class: "form-input-text", style: "height: 120px;", placeholder: "施設詳細を入力" %>
      </div>

      <div class="field">
        <%= f.label :price, class: "form-label" do %>
          宿泊料金<span class="require">*</span>
        <% end %>
        <%= f.number_field :price, class: "form-input-text", placeholder: "一日あたりの料金を入力" %>
      </div>

      <div class="field">
        <%= f.label :address, class: "form-label" do %>
          住所<span class="require">*</span>
        <% end %>
        <%= f.text_field :address, class: "form-input-text", placeholder: "住所を入力" %>
      </div>

      <div class="field">
        <%= f.label :image, "施設画像", class: "form-label" %>
        <%= f.file_field :image, class: "form-input-file" %>
      </div>

      <div class="actions">
        <%= f.submit "更新する", class: "new-submit-btn" %>
      </div>
    <% end %>

    <%= link_to "キャンセル", root_path(@room), class: "cancel-btn" %>
  </div>
</div>
